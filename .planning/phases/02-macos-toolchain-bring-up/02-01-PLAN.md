---
phase: 02-macos-toolchain-bring-up
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .justfile
  - scripts/macos_doctor.sh
autonomous: true
requirements:
  - BOOT-02
user_setup: []
must_haves:
  truths:
    - Contributors can run one canonical macOS doctor command before configuring and see the full prerequisite picture in one pass.
    - The current `PkgConfig` / `PKG_CONFIG_EXECUTABLE` bring-up failure is surfaced as an actionable missing prerequisite instead of only as a later CMake error.
    - The prerequisite report makes the difference between baseline macOS requirements and extra system-mode dependencies explicit, so contributors know when vendored mode is acceptable.
  artifacts:
    - path: .justfile
      provides: Thin contributor-facing `just doctor-macos` entrypoint
      contains: doctor-macos
    - path: scripts/macos_doctor.sh
      provides: Aggregated macOS prerequisite checker for Apple Silicon builds
      contains: pkg-config
  key_links:
    - from: .justfile
      to: scripts/macos_doctor.sh
      via: thin doctor recipe
      pattern: doctor-macos|macos_doctor
    - from: scripts/macos_doctor.sh
      to: CMakeLists.txt
      via: prerequisite checks that preflight the same external gates used by configure
      pattern: PkgConfig|ffmpeg|Vulkan|ncnn|spdlog|Boost
---

<objective>
Add a single macOS doctor/bootstrap command that checks the real Apple Silicon prerequisite surface before contributors hit preset-driven CMake failures.

Purpose: Make prerequisite setup explicit, aggregated, and actionable without turning `just` into a second dependency manager.
Output: A small repo-owned macOS doctor script plus a thin `just` recipe that reports Xcode, Homebrew/PATH, `pkg-config`, FFmpeg, Vulkan SDK or MoltenVK tooling, and system-only dependency readiness.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-surface-foundation/01-02-SUMMARY.md
@.planning/phases/01-build-surface-foundation/01-03-SUMMARY.md
@.planning/phases/02-macos-toolchain-bring-up/02-RESEARCH.md
@CMakeLists.txt
@CMakePresets.json
@.justfile
@README.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement an aggregated Apple Silicon macOS doctor script</name>
  <files>scripts/macos_doctor.sh</files>
  <action>Create a small Bash script at `scripts/macos_doctor.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. The script should run on macOS only, print grouped status sections, and aggregate failures instead of exiting on the first missing item. Check the real prerequisite surface already implied by the repo: Xcode or Command Line Tools availability (`xcode-select`, `xcodebuild`), Homebrew presence and `/opt/homebrew` PATH visibility, `cmake`, `ninja`, `ffmpeg`, `pkg-config` or `pkgconf`, Vulkan SDK or MoltenVK tooling (`vulkaninfo`, `glslangValidator`, relevant SDK env state such as `VULKAN_SDK` when needed), and the extra system-mode packages required by `CMakeLists.txt` (`ncnn`, `spdlog`, Boost program_options`). Treat the currently observed missing `pkg-config` as a concrete bring-up example: the output must call out that this is the same gate behind `Could NOT find PkgConfig` / missing `PKG_CONFIG_EXECUTABLE`. Do not hard-code local machine paths beyond the expected Apple Silicon Homebrew prefix, and do not attempt package installation from the script.</action>
  <verify>Running `bash scripts/macos_doctor.sh` on the current machine prints a consolidated report rather than failing on the first missing executable, and the report explicitly names the `pkg-config` / `PKG_CONFIG_EXECUTABLE` gate when that prerequisite is absent.</verify>
  <done>The repo has a reusable macOS doctor script that reports readiness for both the baseline and system-mode dependency paths with actionable missing-item diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the doctor through a thin canonical `just` entrypoint</name>
  <files>.justfile, scripts/macos_doctor.sh</files>
  <action>Add one canonical recipe, `doctor-macos`, to `.justfile` and keep it thin: it should only delegate to `scripts/macos_doctor.sh` and must not duplicate the actual check logic inline. Place the recipe with the macOS build surface so it is discoverable ahead of packaging helpers, and keep existing configure or build recipe names intact unless a tiny wording or grouping fix is required to keep the doctor entrypoint coherent. Do not add raw `cmake -D...` examples, install commands, or extra wrapper logic to `just`.</action>
  <verify>`just --list --unsorted` shows `doctor-macos` alongside the canonical macOS recipes, and `just --show doctor-macos` reveals only the thin script delegation.</verify>
  <done>Contributors can run exactly one repo-owned command, `just doctor-macos`, to preflight the macOS build prerequisites without reading CMake errors first.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `just --list --unsorted`
- [ ] `just --show doctor-macos`
- [ ] `bash scripts/macos_doctor.sh`
- [ ] If `cmake --preset macos-system-release` still fails, the doctor output identifies the same missing external gate first, starting with `pkg-config` on the current known machine
- [ ] `git diff -- .justfile scripts/macos_doctor.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `just` stays thin and the doctor logic lives in the repo script
- The doctor output distinguishes baseline macOS requirements from extra system-mode dependencies
- Missing prerequisite messaging is concrete enough that contributors do not have to infer setup from `Could NOT find PkgConfig`
</success_criteria>

<output>
After completion, create `.planning/phases/02-macos-toolchain-bring-up/02-01-SUMMARY.md`
</output>
