---
phase: 02-macos-toolchain-bring-up
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - CMakePresets.json
autonomous: true
requirements:
  - BLD-01
  - BLD-02
user_setup: []
must_haves:
  truths:
    - A clean Apple Silicon Release build can be configured and built through the shared preset-backed workflow without custom flag assembly.
    - A clean Apple Silicon Debug build can be configured and built through the shared preset-backed workflow without custom flag assembly.
    - System and vendored dependency modes remain explicit in the presets, and preset-driven failures stop only on real external prerequisites rather than broken preset wiring.
  artifacts:
    - path: CMakePresets.json
      provides: Authoritative macOS arm64 configure and build matrix for system and vendored modes
      contains: macos-system-release
    - path: CMakeLists.txt
      provides: Authoritative CMake dependency and install behavior compatible with the shared presets
      contains: VIDEO2X_USE_EXTERNAL
  key_links:
    - from: CMakePresets.json
      to: CMakeLists.txt
      via: cache variables mapped onto the existing Video2X dependency-mode options
      pattern: VIDEO2X_USE_EXTERNAL|CMAKE_BUILD_TYPE|CMAKE_OSX_ARCHITECTURES
    - from: CMakePresets.json
      to: build/
      via: clean preset-specific binary and install directories used by configure and build
      pattern: binaryDir|configurePreset|buildPresets
---

<objective>
Make the Release and Debug macOS Apple Silicon build flows succeed through the shared presets, fixing only real preset or CMake mismatches and leaving the canonical contributor surface thin.

Purpose: Satisfy the actual build requirements in the authoritative CMake layer instead of papering over issues in `just` or docs.
Output: A validated preset and CMake configuration surface where system Release and Debug builds complete cleanly, and vendored presets remain aligned as the fallback dependency mode.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-surface-foundation/01-02-SUMMARY.md
@.planning/phases/02-macos-toolchain-bring-up/02-RESEARCH.md
@CMakeLists.txt
@CMakePresets.json
@.justfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate and fix configure-time preset wiring from clean build trees</name>
  <files>CMakePresets.json, CMakeLists.txt</files>
  <action>Starting from a clean tree, remove any stale `build/macos-*` preset directories that would hide bring-up issues, then exercise the configure presets that matter to this phase: `macos-system-release`, `macos-system-debug`, `macos-vendored-release`, and `macos-vendored-debug`. Fix only genuine preset or top-level CMake contract mismatches such as broken inheritance, wrong cache variables, inconsistent install paths, or generator assumptions. Keep `CMakeLists.txt` authoritative and leave it untouched if configure works once prerequisites are present. Use the current missing `PkgConfig` failure as the boundary: if configure stops there, treat that as an external prerequisite gate and do not invent new shell-side workarounds for it.</action>
  <verify>From fresh preset build directories, `cmake --preset macos-system-release`, `cmake --preset macos-system-debug`, `cmake --preset macos-vendored-release`, and `cmake --preset macos-vendored-debug` either succeed or fail only on clearly external prerequisites already called out by the doctor flow.</verify>
  <done>The configure presets for both dependency modes are internally sound and no longer require contributors to diagnose preset-level breakage by hand.</done>
</task>

<task type="auto">
  <name>Task 2: Make the system Release and Debug build presets complete end to end</name>
  <files>CMakePresets.json, CMakeLists.txt</files>
  <action>Once configure-time preset wiring is clean and prerequisites are installed, drive the actual build presets for `macos-system-release` and `macos-system-debug` from fresh trees and fix only the minimal preset or top-level CMake issues that prevent a successful `install` target build. Preserve the existing authoritative dependency split: system mode should continue to use external `ncnn`, `spdlog`, and Boost, while vendored mode remains encoded in presets rather than new wrapper commands. Do not move build orchestration into scripts or `just`, and do not expand into runtime smoke tests, packaging, or CI.</action>
  <verify>`cmake --build --preset macos-system-release` and `cmake --build --preset macos-system-debug` complete successfully from a clean checkout after prerequisite readiness is satisfied, and `cmake --list-presets` still shows the full macOS system and vendored matrix.</verify>
  <done>The authoritative CMake preset surface supports one clean Release build and one clean Debug build on Apple Silicon macOS without any raw flag reconstruction.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --list-presets`
- [ ] Fresh-tree `cmake --preset macos-system-release`
- [ ] Fresh-tree `cmake --preset macos-system-debug`
- [ ] Fresh-tree `cmake --preset macos-vendored-release`
- [ ] Fresh-tree `cmake --preset macos-vendored-debug`
- [ ] Fresh-tree `cmake --build --preset macos-system-release`
- [ ] Fresh-tree `cmake --build --preset macos-system-debug`
- [ ] `git diff -- CMakePresets.json CMakeLists.txt`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Release and Debug macOS builds complete through shared presets, not raw flags
- Vendored presets remain intact as the fallback dependency mode and are not allowed to drift from system-mode naming or output layout
- Any code changes stay in authoritative CMake surfaces rather than in thicker shell wrappers
</success_criteria>

<output>
After completion, create `.planning/phases/02-macos-toolchain-bring-up/02-02-SUMMARY.md`
</output>
