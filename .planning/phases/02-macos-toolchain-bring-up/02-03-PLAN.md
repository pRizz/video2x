---
phase: 02-macos-toolchain-bring-up
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - .justfile
  - scripts/macos_doctor.sh
  - README.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - BOOT-02
  - BLD-01
  - BLD-02
user_setup: []
must_haves:
  truths:
    - From a clean checkout, contributors can discover and run the canonical macOS doctor plus build commands without composing raw CMake invocations.
    - The doctor output, `just` recipe names, and contributor docs agree on what system mode requires, what vendored mode still requires, and how the current `PkgConfig` bring-up issue is avoided.
    - The validated contributor path stays focused on prerequisite setup and build execution, without drifting into runtime smoke tests or docs-book scope.
  artifacts:
    - path: .justfile
      provides: Canonical macOS command surface consistent with the validated preset-backed workflow
      contains: doctor-macos
    - path: scripts/macos_doctor.sh
      provides: Prerequisite messaging aligned with the actual configure and build gates
      contains: PKG_CONFIG_EXECUTABLE
    - path: README.md
      provides: Top-level clean-checkout macOS workflow guidance
      contains: configure-macos-system-release
    - path: CONTRIBUTING.md
      provides: Contributor-facing setup and fallback guidance for the validated macOS flow
      contains: vendored
  key_links:
    - from: README.md
      to: .justfile
      via: documented canonical command names
      pattern: doctor-macos|configure-macos|build-macos
    - from: CONTRIBUTING.md
      to: CMakePresets.json
      via: explanation of system versus vendored dependency modes without raw flag examples
      pattern: system|vendored|preset
    - from: scripts/macos_doctor.sh
      to: README.md
      via: matching prerequisite names and fix guidance for the known `PkgConfig` gate
      pattern: pkg-config|Vulkan|MoltenVK
---

<objective>
Validate the real clean-checkout contributor experience end to end, then fix any remaining mismatch between doctor output, `just` entrypoints, and repo guidance.

Purpose: Close the last gap between "the presets technically exist" and "a contributor can actually follow the supported macOS path from a fresh clone."
Output: A validated clean-checkout macOS workflow where the doctor command, Release and Debug `just` commands, and repo docs all describe the same prerequisite and dependency-mode story.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-macos-toolchain-bring-up/02-RESEARCH.md
@.planning/phases/02-macos-toolchain-bring-up/02-01-SUMMARY.md
@.planning/phases/02-macos-toolchain-bring-up/02-02-SUMMARY.md
@.justfile
@scripts/macos_doctor.sh
@README.md
@CONTRIBUTING.md
@CMakePresets.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the clean-checkout macOS contributor matrix through canonical commands</name>
  <files>.justfile, scripts/macos_doctor.sh</files>
  <action>Start from fresh preset build directories and validate the contributor-facing command path only through `just`. Run `just doctor-macos` first, then the canonical system Release and Debug configure or build pairs, and exercise the vendored fallback path enough to prove it is still a valid alternative when system-only packages are not available. Use the known missing `PkgConfig` signal as a concrete mismatch detector: if the doctor output and the first failing configure step disagree about what is missing, treat that as a defect to fix. Do not replace the canonical `just` flow with raw `cmake -D...` commands during validation.</action>
  <verify>From a clean tree, `just doctor-macos`, `just configure-macos-system-release`, `just build-macos-system-release`, `just configure-macos-system-debug`, and `just build-macos-system-debug` succeed once prerequisites are ready, while vendored configure or build commands remain usable as the documented fallback path.</verify>
  <done>The canonical `just` command surface is proven against a fresh checkout rather than assumed from preset existence alone.</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining command-surface and diagnostic mismatches</name>
  <files>.justfile, scripts/macos_doctor.sh</files>
  <action>If clean-checkout validation exposes drift between recipe names, dependency-mode wording, or doctor diagnostics, make the smallest possible fixes in `.justfile` and `scripts/macos_doctor.sh`. Preserve the thin-wrapper rule: `just` may call the doctor script and the shared presets, but it must not duplicate build or dependency logic. Keep the doctor output explicit about which checks are always required versus only required for system mode, and keep the `pkg-config` / `PKG_CONFIG_EXECUTABLE` wording consistent with the real configure failure.</action>
  <verify>`just --list --unsorted`, `just --show doctor-macos`, and a rerun of the clean-checkout command matrix show consistent naming and no recipe-level ambiguity.</verify>
  <done>The command layer and diagnostic layer match the authoritative preset-backed behavior with no hidden dependency-mode surprises.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the validated prerequisite and fallback guidance in repo entrypoints</name>
  <files>README.md, CONTRIBUTING.md</files>
  <action>Update `README.md` and `CONTRIBUTING.md` so they reflect the validated Phase 2 workflow exactly: run `just doctor-macos`, then the canonical system Release or Debug commands, and use the vendored commands only as the documented fallback when system-only packages are not available. Make the prerequisite summary concrete by naming Xcode, Homebrew, `pkg-config`, FFmpeg, Vulkan SDK or MoltenVK tooling, and the extra system-mode packages. Keep these docs scoped to setup and build execution only; do not add runtime smoke steps, packaging instructions, CI discussion, or a docs-book macOS guide here.</action>
  <verify>`rg -n "doctor-macos|configure-macos|build-macos|pkg-config|Vulkan|MoltenVK|vendored" README.md CONTRIBUTING.md` shows the validated command and prerequisite surface, and the wording matches the implemented `just` and preset names.</verify>
  <done>Repo entrypoint docs describe the same clean-checkout macOS workflow that was just validated, including when to choose system versus vendored mode.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Clean-tree `just doctor-macos`
- [ ] Clean-tree `just configure-macos-system-release`
- [ ] Clean-tree `just build-macos-system-release`
- [ ] Clean-tree `just configure-macos-system-debug`
- [ ] Clean-tree `just build-macos-system-debug`
- [ ] Clean-tree vendored fallback check via `just configure-macos-vendored-release` and `just build-macos-vendored-release`, or document why the validated fallback used the Debug variant instead
- [ ] `just --list --unsorted`
- [ ] `rg -n "doctor-macos|configure-macos|build-macos|pkg-config|Vulkan|MoltenVK|vendored" README.md CONTRIBUTING.md .justfile scripts/macos_doctor.sh`
- [ ] `git diff -- .justfile scripts/macos_doctor.sh README.md CONTRIBUTING.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- A contributor can follow the same clean-checkout macOS flow from docs and from `just` without reconstructing raw CMake commands
- System and vendored dependency modes are described consistently across doctor output, recipes, and docs
- Phase 2 stays limited to prerequisite setup and build reproducibility rather than runtime, packaging, CI, or full docs-book scope
</success_criteria>

<output>
After completion, create `.planning/phases/02-macos-toolchain-bring-up/02-03-SUMMARY.md`
</output>
