---
phase: 03-runtime-smoke-and-gpu-proof
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/video2x/include/vulkan_utils.h
  - tools/video2x/src/vulkan_utils.cpp
autonomous: true
requirements:
  - VAL-02
  - GPU-02
user_setup: []
must_haves:
  truths:
    - The built `./build/macos-system-release/video2x --list-devices` command succeeds under the exact passing preconditions already observed locally on 2026-03-24: a built macOS system-release binary plus a working Vulkan portability stack, where output includes a real detected GPU name.
    - When those portability preconditions are not satisfied or regress, the device-list path reports an actionable macOS-specific failure that names the portability requirement instead of only logging a generic instance-creation error.
    - This plan does not widen into CLI exit-code refactoring outside the Vulkan utility path.
  artifacts:
    - path: tools/video2x/src/vulkan_utils.cpp
      provides: portability-aware Vulkan instance creation and macOS device enumeration diagnostics
      contains: VK_INSTANCE_CREATE_ENUMERATE_PORTABILITY_BIT_KHR
    - path: tools/video2x/include/vulkan_utils.h
      provides: shared declaration surface for any helper changes needed by the CLI
      contains: list_vulkan_devices
  key_links:
    - from: tools/video2x/src/vulkan_utils.cpp
      to: build/macos-system-release/video2x
      via: portability-aware instance creation used by the built macOS CLI
      pattern: list_vulkan_devices|vkCreateInstance|VK_KHR_portability_enumeration|MoltenVK
---

<objective>
Make Vulkan device enumeration on macOS portability-aware and explicit so Phase 3 can prove GPU visibility through the actual built CLI.

Purpose: Preserve the now-observed successful `--list-devices` behavior behind explicit portability-aware logic and diagnostics, instead of assuming the local pass will stay stable without the requirement being encoded clearly.
Output: A built `video2x` CLI whose device-list path makes the macOS portability requirement explicit and whose failure mode remains actionable when the local environment drifts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-runtime-smoke-and-gpu-proof/03-RESEARCH.md
@tools/video2x/include/vulkan_utils.h
@tools/video2x/src/vulkan_utils.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode the passing macOS portability preconditions explicitly in the Vulkan helper</name>
  <files>tools/video2x/include/vulkan_utils.h, tools/video2x/src/vulkan_utils.cpp</files>
  <action>Inspect the current successful local `--list-devices` path and make the macOS Vulkan helper explicit about the portability contract instead of relying on implicit loader behavior. On macOS, discover available instance extensions, enable `VK_KHR_portability_enumeration` when advertised, and set `VK_INSTANCE_CREATE_ENUMERATE_PORTABILITY_BIT_KHR` when required by the active portability stack. Preserve non-macOS behavior unless the same extension handling is harmless there, and do not drift into backend rewrites, docs, or unrelated CLI control flow.</action>
  <verify>After rebuilding `macos-system-release`, `./build/macos-system-release/video2x --list-devices` still exits `0` on the current machine and prints at least one detected device, matching the observed 2026-03-24 success path.</verify>
  <done>The current local success case is backed by explicit portability-aware Vulkan helper behavior rather than an implicit environment accident.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten failure diagnostics for portability-regression cases</name>
  <files>tools/video2x/src/vulkan_utils.cpp</files>
  <action>Replace any remaining generic device-list failure messaging with actionable logging that explicitly calls out missing macOS portability requirements when the portability extension is unavailable, instance creation returns an incompatible-driver-style failure, or no portability-backed devices are visible. Keep the scope inside the Vulkan helper only; do not take ownership of `argparse.cpp` or `video2x.cpp` for exit-code work that is already externally correct.</action>
  <verify>If the current machine still has the validated Vulkan portability stack, `./build/macos-system-release/video2x --list-devices` continues to exit `0` and print the detected device. If a negative check can be reproduced safely, the failure mentions `VK_KHR_portability_enumeration`, portability enumeration, or MoltenVK rather than only `Failed to create Vulkan instance.` If a negative runtime check cannot be reproduced locally without destabilizing the machine, confirm the new failure strings in the source diff and record the exact trigger conditions in the summary.</verify>
  <done>The success path remains green on the current machine, and the fallback failure path is explicit about the macOS portability requirement instead of being a generic Vulkan dead end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Fresh `cmake --build --preset macos-system-release`
- [ ] `./build/macos-system-release/video2x --list-devices`
- [ ] On the current machine, `--list-devices` exits `0` and output includes a detected GPU name
- [ ] If a safe negative runtime check is possible, the failing output names `VK_KHR_portability_enumeration`, portability enumeration, or MoltenVK explicitly
- [ ] If a safe negative runtime check is not possible, the source diff contains the new portability-specific failure strings and the summary records the passing preconditions observed on 2026-03-24
- [ ] `git diff -- tools/video2x/include/vulkan_utils.h tools/video2x/src/vulkan_utils.cpp`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The current successful macOS device-list path is preserved and made portability-aware explicitly
- Missing-portability failures are actionable instead of generic
- The plan stays scoped to the Vulkan helper and does not claim unnecessary CLI exit-code ownership
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-smoke-and-gpu-proof/03-02-SUMMARY.md`
</output>
