---
phase: 03-runtime-smoke-and-gpu-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fsutils.cpp
  - CMakeLists.txt
autonomous: true
requirements:
  - VAL-01
user_setup: []
must_haves:
  truths:
    - `src/fsutils.cpp` has a real macOS executable-directory implementation instead of the current `/proc/self/exe` path, so runtime probes no longer emit `Error reading /proc/self/exe: No such file or directory`.
    - The built macOS runtime proof no longer depends on launching from the repo root to discover checked-in model data.
    - Shared runtime data needed by processor paths is discoverable from the built-tree layout through executable-relative lookup, with installed-layout hardening treated as secondary validation.
  artifacts:
    - path: src/fsutils.cpp
      provides: real macOS executable-directory resolution plus resource lookup that recognizes built and installed layouts
      contains: _NSGetExecutablePath
    - path: CMakeLists.txt
      provides: optional installed-artifact runtime hardening for the macOS CLI
      contains: install(TARGETS video2x
  key_links:
    - from: src/fsutils.cpp
      to: build/macos-system-release/video2x
      via: executable-directory discovery used by built-binary runtime probes on macOS
      pattern: _NSGetExecutablePath|proc_pidpath|get_executable_directory|models
    - from: src/fsutils.cpp
      to: build/install/macos-system-release/share/video2x/models
      via: secondary candidate paths rooted at the macOS install prefix instead of Linux-only defaults
      pattern: models|share/video2x|get_executable_directory
    - from: CMakeLists.txt
      to: build/install/macos-system-release/bin/video2x
      via: optional loader-relative runtime hardening for the installed CLI and shared library
      pattern: RPATH|INSTALL_RPATH|BUILD_RPATH|@loader_path|@executable_path
---

<objective>
Repair the built macOS runtime path contract so Phase 3 proof commands work from a clean working directory instead of depending on repo-root-relative model discovery.

Purpose: Fix the explicit macOS executable-path bug in `fsutils` and make the built CLI discover its runtime assets through executable-relative lookup, while treating installed-artifact hardening as secondary.
Output: A built `video2x` binary that launches cleanly on macOS, no longer hits `/proc/self/exe`, and can find the model data needed by sample workloads without assuming the caller is in the repo root.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-runtime-smoke-and-gpu-proof/03-RESEARCH.md
@CMakeLists.txt
@src/fsutils.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Linux-only executable-path lookup with a real macOS implementation</name>
  <files>src/fsutils.cpp</files>
  <action>Update `src/fsutils.cpp` so `get_executable_directory()` uses a real macOS executable-path API instead of the current non-Windows fallback that reads `/proc/self/exe`. Keep Linux behavior intact, but add an Apple-specific path implementation and make resource lookup explicit about the built-tree macOS layout so model discovery works through executable-relative paths, not only through repo-root-relative `models/...` lookups. The goal is to remove the known live failure signal and make built-binary runtime proof independent of the caller's current working directory.</action>
  <verify>After a fresh `cmake --build --preset macos-system-release`, `./build/macos-system-release/video2x --help` exits `0`, and from a temp directory outside the repo root a synthetic workload such as `cd /tmp/video2x-phase3 && /Users/peterryszkiewicz/Repos/video2x/build/macos-system-release/video2x -i input.mp4 -o realesrgan-fsutils.mp4 -p realesrgan -s 2 --realesrgan-model realesr-animevideov3 --no-progress` no longer emits `Error reading /proc/self/exe: No such file or directory` and no longer depends on repo-root-relative model lookup.</verify>
  <done>The macOS runtime path helper no longer relies on Linux-only `/proc/self/exe` behavior and the built CLI can resolve its runtime assets through a platform-correct executable-relative implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Optionally harden the installed macOS artifact if the fix stays small and repo-owned</name>
  <files>CMakeLists.txt, src/fsutils.cpp</files>
  <action>If, after fixing the executable-path bug, the installed artifact still fails only because of missing loader-relative runtime linkage or install-prefix resource lookup, make the smallest repo-owned CMake and fsutils adjustments needed to harden that path. Keep this explicitly secondary: do not let installed-artifact work delay the built-binary smoke baseline, and do not broaden into packaging, bundles, or release distribution concerns.</action>
  <verify>If this hardening is attempted, `./build/install/macos-system-release/bin/video2x --help` exits `0` and any added macOS install-layout search path is visible in the source diff or binary metadata. If the installed artifact remains out of scope after the built-binary baseline is fixed, document that boundary clearly in the summary instead of treating it as a failed phase gate.</verify>
  <done>The installed path is either hardened with a small repo-owned fix or explicitly documented as secondary to the built-binary validation contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Fresh `cmake --build --preset macos-system-release`
- [ ] `./build/macos-system-release/video2x --help` exits `0`
- [ ] `mkdir -p /tmp/video2x-phase3 && ffmpeg -y -f lavfi -i testsrc=size=320x180:rate=12 -t 1 /tmp/video2x-phase3/input.mp4`
- [ ] `cd /tmp/video2x-phase3 && /Users/peterryszkiewicz/Repos/video2x/build/macos-system-release/video2x -i input.mp4 -o realesrgan-fsutils.mp4 -p realesrgan -s 2 --realesrgan-model realesr-animevideov3 --no-progress`
- [ ] The out-of-tree runtime probe above no longer logs `Error reading /proc/self/exe: No such file or directory`
- [ ] The out-of-tree runtime probe above succeeds without relying on repo-root-relative `models/...`
- [ ] `git diff -- src/fsutils.cpp CMakeLists.txt`
- [ ] If installed-artifact hardening is in scope, `./build/install/macos-system-release/bin/video2x --help` exits `0`
- [ ] If installed-artifact hardening is in scope, `otool -l ./build/install/macos-system-release/bin/video2x | rg -n "LC_RPATH|@loader_path|@executable_path"`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The macOS executable-path implementation is platform-correct and the `/proc/self/exe` failure is gone
- The built macOS runtime proof no longer depends on the repo root as the caller's working directory
- Any installed-artifact work remains secondary hardening rather than an implicit phase requirement
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-smoke-and-gpu-proof/03-01-SUMMARY.md`
</output>
