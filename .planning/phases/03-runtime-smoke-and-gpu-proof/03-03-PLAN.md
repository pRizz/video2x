---
phase: 03-runtime-smoke-and-gpu-proof
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - .justfile
  - scripts/macos_runtime_validation.sh
  - README.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-03
  - GPU-02
user_setup: []
must_haves:
  truths:
    - Contributors can run canonical macOS smoke, device-list, and short sample-workload commands from the repo root without reusing Linux-only `LD_LIBRARY_PATH` recipes.
    - The canonical Phase 3 proof target is the built `build/macos-system-release/video2x` binary; any installed-artifact checks are secondary and must be labeled that way.
    - The sample validation flow does not assume `data/standard-test.mp4` exists in the repo; it creates a short local input clip with `ffmpeg` or accepts an explicit input path, and it uses a validated processor path instead of assuming libplacebo works on macOS today.
    - The validation flow and docs make the Vulkan portability requirement on macOS explicit and state the exact passing preconditions behind `--list-devices`.
  artifacts:
    - path: .justfile
      provides: canonical Phase 3 macOS validation commands exposed through thin `just` recipes
      contains: smoke-macos
    - path: scripts/macos_runtime_validation.sh
      provides: repo-owned smoke, device-list, and short sample validation logic for macOS
      contains: ffprobe
    - path: README.md
      provides: top-level macOS runtime validation guidance
      contains: MoltenVK
    - path: CONTRIBUTING.md
      provides: contributor-facing Phase 3 smoke and workload guidance with a validated processor choice
      contains: sample-macos-realesrgan
  key_links:
    - from: .justfile
      to: scripts/macos_runtime_validation.sh
      via: thin macOS validation recipe delegation
      pattern: smoke-macos|list-devices-macos|sample-macos-realesrgan
    - from: scripts/macos_runtime_validation.sh
      to: build/macos-system-release/video2x
      via: runtime checks against the built CLI first, plus optional installed-artifact mode only if supported
      pattern: --help|--list-devices|ffmpeg -y -f lavfi -i testsrc=size=320x180:rate=12 -t 1|ffprobe|sample-macos-realesrgan|realcugan
    - from: README.md
      to: CONTRIBUTING.md
      via: matching command names and explicit MoltenVK portability guidance
      pattern: smoke-macos|list-devices-macos|sample-macos-realesrgan|portability|detected GPU
---

<objective>
Publish the canonical Phase 3 macOS runtime validation flow so contributors can prove smoke launch, device enumeration, and one short sample workload from the repo root.

Purpose: Turn the first two plans' runtime and portability results into an executable contributor workflow with exact commands and proof criteria, using the built binary as the primary contract and a validated ncnn-backed sample workload rather than a brittle libplacebo assumption.
Output: Thin `just` commands plus a repo-owned validation script that exercise the built macOS CLI, generate or accept a short input clip locally, and document the portability-aware validation path clearly.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-runtime-smoke-and-gpu-proof/03-RESEARCH.md
@.planning/phases/03-runtime-smoke-and-gpu-proof/03-01-SUMMARY.md
@.planning/phases/03-runtime-smoke-and-gpu-proof/03-02-SUMMARY.md
@.justfile
@README.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a repo-owned macOS runtime validation script</name>
  <files>scripts/macos_runtime_validation.sh</files>
  <action>Create `scripts/macos_runtime_validation.sh` as a Bash script with `#!/usr/bin/env bash` and `set -euo pipefail`. The script should provide explicit subcommands for smoke launch, device listing, and a short sample workload. Default the target binary to `build/macos-system-release/video2x`, treat any installed-artifact mode as optional and secondary, and do not assume `data/standard-test.mp4` exists. For the canonical sample workload, either accept a user-supplied input clip or generate a tiny local clip with `ffmpeg -y -f lavfi -i testsrc=size=320x180:rate=12 -t 1 ...`, then run a validated `realesrgan` path and use `ffprobe` to confirm the output file exists and contains video output. If helpful, the script may also offer `realcugan` as a validated fallback, but libplacebo must not remain the default Phase 3 sample unless it can be proven locally.</action>
  <verify>Running the script directly can perform all three checks from the repo root: smoke launch returns success, device listing delegates to `--list-devices`, and the sample workload leaves behind a verifiable output file that `ffprobe` can inspect.</verify>
  <done>The repo has one reusable macOS validation entrypoint that covers launch, device enumeration, and short sample proof without depending on Linux-specific test recipes or checked-in sample media.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the Phase 3 validation flow through thin `just` commands</name>
  <files>.justfile, scripts/macos_runtime_validation.sh</files>
  <action>Add canonical macOS validation recipes to `.justfile` that stay thin and only delegate to `scripts/macos_runtime_validation.sh`. Use explicit command names for the three proof points: `smoke-macos`, `list-devices-macos`, and `sample-macos-realesrgan`. If helpful, add `sample-macos-realcugan` as a secondary validated fallback, but do not make libplacebo the only sample command unless it has been revalidated locally. Place these recipes with the macOS workflow surface instead of the existing Linux-oriented `test-*` recipes, and do not embed the validation logic inline in `.justfile` or repurpose the current `LD_LIBRARY_PATH` test recipes as if they were valid macOS runtime coverage.</action>
  <verify>`just --list --unsorted` shows the new macOS validation commands, and `just --show smoke-macos`, `just --show list-devices-macos`, and `just --show sample-macos-realesrgan` each reveal only thin delegation into the script.</verify>
  <done>Contributors get canonical Phase 3 commands through `just` without having to remember script paths or adapt Linux runtime recipes for macOS.</done>
</task>

<task type="auto">
  <name>Task 3: Document the validated macOS smoke, device, and sample workload path</name>
  <files>README.md, CONTRIBUTING.md</files>
  <action>Update `README.md` and `CONTRIBUTING.md` so Phase 3 guidance matches the implemented validation flow exactly: name the canonical smoke, device-list, and short sample commands; explain that the default proof target is the built macOS CLI and that installed-artifact checks, if present, are secondary; and call out the Vulkan portability requirement on macOS in plain language, including MoltenVK or equivalent tooling and the exact passing preconditions behind the observed `--list-devices` success. Phrase the proof generically around a detected GPU name instead of one host-specific device model. Also make the sample-media story explicit: no repo-local sample clip is assumed, so the validation flow either generates one locally with `ffmpeg` or accepts a provided path. If libplacebo is documented at all, mark it as optional or currently non-canonical until it is revalidated.</action>
  <verify>`rg -n "smoke-macos|list-devices-macos|sample-macos-(realesrgan|realcugan)|MoltenVK|portability|detected GPU|ffmpeg|ffprobe|built binary" README.md CONTRIBUTING.md .justfile scripts/macos_runtime_validation.sh` shows one coherent Phase 3 command and validation story across implementation and docs.</verify>
  <done>The repo entrypoints describe the same portability-aware runtime validation flow that contributors can actually execute from the root of the repo.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `just configure-macos-system-release`
- [ ] `just build-macos-system-release`
- [ ] `just --list --unsorted`
- [ ] `just --show smoke-macos`
- [ ] `just --show list-devices-macos`
- [ ] `just --show sample-macos-realesrgan`
- [ ] `just smoke-macos`
- [ ] `just list-devices-macos`
- [ ] `just sample-macos-realesrgan`
- [ ] The sample input can be generated locally with `ffmpeg -y -f lavfi -i testsrc=size=320x180:rate=12 -t 1 ...` and does not require network access
- [ ] The sample validation confirms output with `ffprobe` and leaves behind a non-empty output file
- [ ] `rg -n "smoke-macos|list-devices-macos|sample-macos-(realesrgan|realcugan)|MoltenVK|portability|detected GPU|ffmpeg|ffprobe|built binary" README.md CONTRIBUTING.md .justfile scripts/macos_runtime_validation.sh`
- [ ] `git diff -- .justfile scripts/macos_runtime_validation.sh README.md CONTRIBUTING.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- macOS contributors have canonical smoke, device-list, and sample-workload commands
- The sample proof is self-contained, uses a validated processor path, and does not rely on a checked-in test video
- The built macOS binary is clearly identified as the primary validation target
- The Vulkan portability requirement is explicit in both validation behavior and docs
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-smoke-and-gpu-proof/03-03-SUMMARY.md`
</output>
