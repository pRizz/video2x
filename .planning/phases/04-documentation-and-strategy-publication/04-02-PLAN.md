---
phase: 04-documentation-and-strategy-publication
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/book/src/developing/macos-gpu-strategy.md
autonomous: true
requirements:
  - GPU-01
user_setup: []
must_haves:
  truths:
    - The project docs explicitly define the initial macOS GPU strategy as Vulkan SDK plus MoltenVK over Metal, rather than leaving contributors to infer it from code or past discussion.
    - The strategy page explains why MoltenVK is the first route for this fork: the current runtime and processor stack are already Vulkan-oriented, and native Metal work is not yet justified.
    - The page records the current assumptions, portability requirements, and evidence gates for later optimization work without collapsing Phase 4 into a Phase 5 recommendation.
  artifacts:
    - path: docs/book/src/developing/macos-gpu-strategy.md
      provides: dedicated macOS GPU strategy page in project docs
      contains: MoltenVK
  key_links:
    - from: docs/book/src/developing/macos-gpu-strategy.md
      to: tools/video2x/src/vulkan_utils.cpp
      via: documented portability-aware Vulkan path that matches the implemented runtime contract
      pattern: VK_KHR_portability_enumeration|MoltenVK|Vulkan SDK
    - from: docs/book/src/developing/macos-gpu-strategy.md
      to: .planning/phases/03-runtime-smoke-and-gpu-proof/03-VERIFICATION.md
      via: strategy language grounded in the already verified smoke, device-list, and sample workload evidence
      pattern: smoke-macos|list-devices-macos|sample-macos-realesrgan|Apple Silicon
---

<objective>
Publish the initial macOS GPU strategy so contributors understand what backend path this fork is actually betting on and what would have to go wrong before deeper native work becomes justified.

Purpose: Turn the earlier research and the now-verified Phase 3 runtime behavior into project documentation that names MoltenVK and Vulkan SDK as the current strategy, explains the rationale, and records the current boundaries and caveats.
Output: A dedicated `docs/book/src/developing/macos-gpu-strategy.md` page covering the chosen GPU path, portability requirements, known limits, and the explicit gate for later Metal or VideoToolbox exploration.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/03-runtime-smoke-and-gpu-proof/03-VERIFICATION.md
@README.md
@CONTRIBUTING.md
@tools/video2x/src/vulkan_utils.cpp
@scripts/macos_runtime_validation.sh
@docs/book/src/developing/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the macOS GPU strategy page around the validated MoltenVK path</name>
  <files>docs/book/src/developing/macos-gpu-strategy.md</files>
  <action>Create `docs/book/src/developing/macos-gpu-strategy.md` and make it explicit that the current first-class macOS GPU route is Vulkan SDK plus MoltenVK. Explain why this is the shortest defensible path for this fork: the existing Video2X processing stack is already Vulkan-shaped through ncnn and related runtime behavior, the current runtime proof succeeded on Apple Silicon, and a native Metal rewrite would widen scope before it is justified.</action>
  <verify>`rg -n "Vulkan SDK|MoltenVK|Apple Silicon|ncnn|Vulkan|Metal" docs/book/src/developing/macos-gpu-strategy.md` shows the chosen strategy, the Apple Silicon target, and the rationale for not starting with native Metal.</verify>
  <done>The docs explicitly name the current macOS GPU strategy and its rationale instead of leaving that decision implicit.</done>
</task>

<task type="auto">
  <name>Task 2: Document portability requirements, current caveats, and future-evidence gates</name>
  <files>docs/book/src/developing/macos-gpu-strategy.md</files>
  <action>Expand the page with the operational details contributors and future maintainers need: `VK_KHR_portability_enumeration` and MoltenVK-equivalent tooling are part of the contract; Phase 3's smoke, device-list, and Real-ESRGAN sample commands are the current proof surface; duplicate MoltenVK installs and non-fatal install-time rpath noise are known caveats; VideoToolbox and native Metal remain optional future work only if measured limitations or missing capability make the portability path insufficient.</action>
  <verify>`rg -n "VK_KHR_portability_enumeration|smoke-macos|list-devices-macos|sample-macos-realesrgan|VideoToolbox|Metal|caveat|warning|Phase 5" docs/book/src/developing/macos-gpu-strategy.md` shows the portability contract, current proof commands, known caveats, and the gate for deeper backend work.</verify>
  <done>The strategy page records the current assumptions and future-decision triggers without pretending Phase 5 has already been decided.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f docs/book/src/developing/macos-gpu-strategy.md`
- [ ] `rg -n "Vulkan SDK|MoltenVK|Apple Silicon|ncnn|Metal" docs/book/src/developing/macos-gpu-strategy.md`
- [ ] `rg -n "VK_KHR_portability_enumeration|smoke-macos|list-devices-macos|sample-macos-realesrgan|VideoToolbox|Phase 5|warning|caveat" docs/book/src/developing/macos-gpu-strategy.md`
- [ ] `git diff -- docs/book/src/developing/macos-gpu-strategy.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The project docs explicitly define the initial macOS GPU strategy around Vulkan SDK plus MoltenVK
- The rationale for deferring native Metal or VideoToolbox-first work is documented clearly and defensibly
- The current portability requirements, caveats, and future evidence gate are recorded without widening scope into optimization work
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-and-strategy-publication/04-02-SUMMARY.md`
</output>
