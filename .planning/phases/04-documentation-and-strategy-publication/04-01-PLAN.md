---
phase: 04-documentation-and-strategy-publication
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/book/src/building/macos.md
autonomous: true
requirements:
  - DOC-01
user_setup: []
must_haves:
  truths:
    - `docs/book` contains a real macOS build guide instead of only a shallow placeholder that points readers back to root docs.
    - The macOS build guide matches the validated contributor contract from Phases 2 and 3: `just doctor-macos`, preset-backed configure and build commands, and a built-binary-first validation handoff.
    - The guide states the actual support boundary for this fork: latest macOS on Apple Silicon, system-mode Release and Debug as the validated path, and vendored mode as a fallback rather than the primary story.
  artifacts:
    - path: docs/book/src/building/macos.md
      provides: first-class macOS Apple Silicon build guide in the docs book
      contains: just doctor-macos
  key_links:
    - from: docs/book/src/building/macos.md
      to: .justfile
      via: canonical configure, build, and runtime-validation command names documented in one place
      pattern: doctor-macos|configure-macos-system-release|build-macos-system-release|smoke-macos|list-devices-macos|sample-macos-realesrgan
    - from: docs/book/src/building/macos.md
      to: CONTRIBUTING.md
      via: matching prerequisites and support-boundary language for the validated macOS workflow
      pattern: Apple Silicon|latest macOS|vendored|third_party/boost
---

<objective>
Create the first-class macOS build guide in `docs/book` so contributors can learn the validated Apple Silicon workflow without having to reconstruct it from `README.md` and `CONTRIBUTING.md`.

Purpose: Move the macOS source-build story into the documentation book with parity to the existing Linux and Windows build pages, while preserving the repo's actual validated command surface.
Output: A dedicated `docs/book/src/building/macos.md` page that covers prerequisites, support boundaries, canonical `just` commands, system-versus-vendored expectations, and the handoff into runtime validation.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation-and-strategy-publication/04-RESEARCH.md
@README.md
@CONTRIBUTING.md
@.justfile
@docs/book/src/building/README.md
@docs/book/src/building/linux.md
@docs/book/src/building/windows.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write a full macOS Apple Silicon build guide page in the docs book</name>
  <files>docs/book/src/building/macos.md</files>
  <action>Create `docs/book/src/building/macos.md` as a first-class build guide for this fork. Make it specific to the validated Apple Silicon contract: prerequisite surface, `just doctor-macos`, system-mode Release and Debug configure/build flows, vendored fallback expectations, and the current support boundary of latest macOS on Apple Silicon only. Keep the wording concrete and repo-accurate; do not introduce raw CMake commands as the canonical contributor path or imply packaging, CI, or Intel Mac support.</action>
  <verify>`rg -n "doctor-macos|configure-macos-system-release|build-macos-system-release|configure-macos-system-debug|build-macos-system-debug|Apple Silicon|latest macOS|vendored|third_party" docs/book/src/building/macos.md` shows the validated prerequisites, command names, and support boundary in the new guide.</verify>
  <done>The docs book has a macOS guide page that reflects the implemented contributor workflow instead of pointing readers to scattered root docs.</done>
</task>

<task type="auto">
  <name>Task 2: Make the guide explicit about runtime handoff and non-goals</name>
  <files>docs/book/src/building/macos.md</files>
  <action>Extend the new page so it does not stop at compilation. Call out the Phase 3 validation handoff using `smoke-macos`, `list-devices-macos`, and `sample-macos-realesrgan`, and explain the current non-goals and caveats that matter to contributors: vendored mode is fallback-only, `third_party/boost` checkout state may matter locally, packaging and macOS CI are not part of v1, and libplacebo is not the canonical macOS proof path.</action>
  <verify>`rg -n "smoke-macos|list-devices-macos|sample-macos-realesrgan|packaging|CI|libplacebo|third_party/boost" docs/book/src/building/macos.md` shows the runtime handoff plus the current scope boundaries and caveats.</verify>
  <done>The macOS guide explains both how to build and how to know the build is ready for the validated runtime path, without overstating current support.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f docs/book/src/building/macos.md`
- [ ] `rg -n "doctor-macos|configure-macos-system-release|build-macos-system-release|configure-macos-system-debug|build-macos-system-debug|Apple Silicon|latest macOS|vendored|third_party" docs/book/src/building/macos.md`
- [ ] `rg -n "smoke-macos|list-devices-macos|sample-macos-realesrgan|libplacebo|packaging|CI" docs/book/src/building/macos.md`
- [ ] `git diff -- docs/book/src/building/macos.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The docs book now contains a real macOS build guide page with parity to the existing platform guide model
- The guide documents the validated Apple Silicon workflow, not an aspirational or raw-CMake alternative
- The guide hands contributors off to the real runtime proof commands and states the current support boundaries clearly
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-and-strategy-publication/04-01-SUMMARY.md`
</output>
