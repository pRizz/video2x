---
phase: 01-build-surface-foundation
plan: "01"
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - .justfile
autonomous: true
requirements:
  - BOOT-01
user_setup: []
must_haves:
  truths:
    - The canonical macOS contributor recipes in `just --list` use preset-backed verbs instead of embedding raw `cmake -D...` bundles.
    - The `.justfile` keeps packaging and test commands available without presenting them as the primary macOS build workflow.
    - A contributor can discover the supported macOS release and debug entrypoints from `.justfile` alone.
  artifacts:
    - path: .justfile
      provides: Thin contributor-facing macOS recipes and grouped command discovery
      contains: cmake --preset
  key_links:
    - from: .justfile
      to: CMakePresets.json
      via: preset names forwarded by canonical macOS recipes
      pattern: cmake --(?:build\\s+)?--preset
---

<objective>
Normalize `.justfile` into a thin macOS-first front door that forwards to the shared CMake presets created in Plan 01-02 instead of duplicating configure logic.

Purpose: Make `just` discoverable and canonical for contributors without turning it into a second build system.
Output: A simplified `.justfile` whose primary macOS recipes wrap named presets and clearly separate supporting packaging or test commands.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-surface-foundation/01-02-SUMMARY.md
@.planning/phases/01-build-surface-foundation/01-RESEARCH.md
@.justfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace raw macOS configure/build bundles with preset-backed recipes</name>
  <files>.justfile</files>
  <action>Use the preset names and output directories delivered by Plan 01-02, then rewrite the canonical macOS contributor recipes so they call `cmake --preset ...` and `cmake --build --preset ...` rather than assembling repeated `-D...` flags in the recipe body. Provide explicit release and debug entrypoints for the supported dependency modes from Plan 01-02, and keep recipe names stable and self-explanatory such as `configure-macos-release`, `build-macos-release`, or similarly clear names. Do not add shell logic that re-encodes dependency selection, compiler flags, install layout, or architecture targeting in `just`; those choices must stay in `CMakePresets.json`.</action>
  <verify>`just --list --unsorted` shows the canonical macOS recipes, and `just --show <each canonical macOS recipe>` shows only preset-forwarding commands rather than raw `cmake -D...` bundles.</verify>
  <done>The primary macOS recipes in `.justfile` are thin wrappers over shared presets for release/debug and the chosen dependency modes.</done>
</task>

<task type="auto">
  <name>Task 2: Reorganize `.justfile` around canonical workflow discovery</name>
  <files>.justfile</files>
  <action>Restructure recipe ordering, grouping, aliases, and terse comments so the supported macOS contributor path is the first thing a contributor sees, while packaging, distro bootstrap, and test helpers remain available but clearly secondary. Preserve existing non-macOS recipes unless they directly conflict with the new canonical surface. If a default or help recipe is added, keep it thin and informational only; it must not duplicate build logic or maintain a separate command matrix.</action>
  <verify>`just --list` presents the macOS build entrypoints before packaging or test recipes, and a diff review confirms the duplicated raw macOS configure flags were removed from `.justfile`.</verify>
  <done>`.justfile` exposes one obvious macOS-first workflow and no longer makes contributors infer the canonical path from mixed build, package, and test recipes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `just --list --unsorted`
- [ ] `just --show <each canonical macOS recipe>`
- [ ] `git diff -- .justfile` confirms canonical macOS recipes forward to presets instead of raw flag bundles
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No new recipe duplicates reintroduce raw macOS configure logic
- `.justfile` is the thin front door for the supported macOS workflow rather than a second build system
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-surface-foundation/01-01-SUMMARY.md`
</output>
