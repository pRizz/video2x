---
phase: 01-build-surface-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakePresets.json
  - CMakeLists.txt
autonomous: true
requirements:
  - BLD-03
user_setup: []
must_haves:
  truths:
    - Shared CMake presets define the supported latest-macOS Apple Silicon configure and build variants for this fork.
    - The preset matrix expresses both build type and supported dependency mode choices without requiring contributors to hand-assemble raw cache flags.
    - Preset-driven configure and build commands work through CMake's native preset interface instead of ad hoc shell wrappers.
  artifacts:
    - path: CMakePresets.json
      provides: Checked-in shared configure/build presets for supported macOS variants
      contains: macos
    - path: CMakeLists.txt
      provides: Preset-compatible cache option and install behavior adjustments when needed
      contains: VIDEO2X_USE_EXTERNAL
  key_links:
    - from: CMakePresets.json
      to: CMakeLists.txt
      via: cacheVariables mapped onto existing Video2X options
      pattern: VIDEO2X_(USE_EXTERNAL|ENABLE|BUILD)
    - from: CMakePresets.json
      to: build/
      via: named configurePreset and buildPreset definitions
      pattern: configurePresets|buildPresets
---

<objective>
Introduce shared CMake presets for the supported macOS Apple Silicon build matrix and make only the minimum CMake-side adjustments needed for preset-driven configuration.

Purpose: Move canonical build configuration detail out of `.justfile` and into CMake's shared preset surface.
Output: A checked-in `CMakePresets.json` that defines the supported macOS release/debug plus dependency-mode variants, with any minimal `CMakeLists.txt` cleanup required to keep those presets valid.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-surface-foundation/01-RESEARCH.md
@CMakeLists.txt
@.justfile
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the supported macOS preset matrix</name>
  <files>CMakePresets.json</files>
  <action>Create a checked-in `CMakePresets.json` for latest macOS on Apple Silicon only. Use a small inheritance chain with one hidden base macOS preset, then explicit configure presets for release/debug and the supported dependency modes the current repo can actually build today. Prefer a matrix that covers `system` dependencies and one vendored fallback mode using the existing `VIDEO2X_USE_EXTERNAL_*` options, rather than inventing more modes. Include matching build presets, stable binary/install directory conventions under `build/`, and any generator or architecture settings that belong in shared CMake configuration. Do not add `CMakeUserPresets.json`, local machine paths, or runtime validation commands.</action>
  <verify>`cmake --list-presets` shows the new macOS configure/build presets, and the preset names make the build type and dependency mode obvious without opening `.justfile`.</verify>
  <done>`CMakePresets.json` expresses the supported macOS build matrix in CMake-native shared configuration with no hand-assembled contributor flags required.</done>
</task>

<task type="auto">
  <name>Task 2: Keep CMake authoritative for preset-selected behavior</name>
  <files>CMakeLists.txt, CMakePresets.json</files>
  <action>Run preset-driven configure checks and fix only CMake-side issues that prevent the new preset matrix from working cleanly. Keep fixes narrow: cache option consistency, install-path defaults, generator assumptions, or other brownfield build-surface issues that are better solved in CMake than in shell recipes. Preserve existing runtime, packaging, and GPU logic. If `CMakeLists.txt` already supports the preset matrix cleanly, leave it unchanged and record that in the summary instead of making cosmetic edits.</action>
  <verify>From a clean tree, `cmake --preset <canonical-macos-release-preset>` completes or fails only on external dependency availability, and `cmake --preset <canonical-macos-vendored-debug-preset>` configures without requiring manual cache flags.</verify>
  <done>The supported macOS presets map directly onto authoritative CMake options, and any required compatibility fixes live in CMake rather than in recipe-specific shell logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --list-presets`
- [ ] `cmake --preset <canonical macOS release preset>`
- [ ] `cmake --preset <canonical macOS vendored debug preset>`
- [ ] `git diff -- CMakePresets.json CMakeLists.txt` shows the shared config moved into presets, not shell wrappers
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The macOS preset matrix is minimal, explicit, and limited to supported Apple Silicon variants
- Shared presets now carry build-type and dependency-mode detail that contributors previously had to reconstruct from raw flags
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-surface-foundation/01-02-SUMMARY.md`
</output>
