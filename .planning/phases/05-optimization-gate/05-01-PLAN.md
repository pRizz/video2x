---
phase: 05-optimization-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .justfile
  - scripts/macos_benchmark.sh
  - .planning/phases/05-optimization-gate/05-01-BASELINE.md
autonomous: true
requirements:
  - GPU-03
user_setup: []
must_haves:
  truths:
    - The repo exposes a repeatable macOS benchmark surface around the existing `video2x --benchmark` mode instead of inventing a second timing mechanism.
    - The benchmark surface makes the current benchmark-mode friction explicit and contributor-safe on macOS: it must handle or document the current requirement for a dummy `--output` path even though the benchmark path discards frame writes.
    - The phase records at least one real filtering-path and one real interpolation-path baseline or explicitly documents why a planned path could not be benchmarked on the current host.
    - The baseline evidence distinguishes benchmark-mode throughput from end-to-end runtime proof so later backend decisions are not made from misleading numbers.
    - The recorded benchmark evidence is quality-gated: if a run is too short to produce a meaningful elapsed-time summary, the workflow must rerun with a longer clip or mark that result as insufficient instead of treating it as valid evidence.
  artifacts:
    - path: scripts/macos_benchmark.sh
      provides: repo-owned macOS benchmark wrapper around the existing `--benchmark` CLI path
      contains: --benchmark
    - path: .justfile
      provides: discoverable macOS benchmark recipes for the current Apple Silicon workflow
      contains: benchmark-macos
    - path: .planning/phases/05-optimization-gate/05-01-BASELINE.md
      provides: recorded baseline results plus observed bottlenecks and benchmark caveats
      contains: Average processing speed
  key_links:
    - from: scripts/macos_benchmark.sh
      to: build/macos-system-release/video2x
      via: benchmark execution against the canonical built macOS binary and its current `--benchmark` behavior
      pattern: --benchmark|--output|realesrgan|rife|ffmpeg|Average processing speed
    - from: .planning/phases/05-optimization-gate/05-01-BASELINE.md
      to: scripts/macos_benchmark.sh
      via: baseline observations captured from the repo-owned benchmark workflow rather than ad hoc shell commands
      pattern: benchmark|Average processing speed|Apple Silicon|MoltenVK|bottleneck
---

<objective>
Capture measured or observed limitations in the initial Apple Silicon bring-up by formalizing a reproducible benchmark surface around the benchmark mode the CLI already exposes.

Purpose: Turn the latent `--benchmark` path into a contributor-facing macOS benchmark workflow, record real baseline evidence, and make any benchmark-specific caveats explicit before the project debates deeper backend work.
Output: Thin benchmark recipes plus a repo-owned benchmark helper script and a baseline evidence document that records current Apple Silicon bottlenecks, benchmark quirks, and comparative observations.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-optimization-gate/05-RESEARCH.md
@.planning/phases/03-runtime-smoke-and-gpu-proof/03-VERIFICATION.md
@.planning/phases/04-documentation-and-strategy-publication/04-VERIFICATION.md
@.justfile
@README.md
@scripts/macos_runtime_validation.sh
@tools/video2x/src/argparse.cpp
@tools/video2x/src/video2x.cpp
@src/libvideo2x.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose a thin macOS benchmark workflow around the existing benchmark mode</name>
  <files>.justfile, scripts/macos_benchmark.sh</files>
  <action>Create a repo-owned `scripts/macos_benchmark.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`, following the same style as `scripts/macos_runtime_validation.sh`. Do not invent new timing logic. Wrap the existing `video2x --benchmark` path so contributors can run reproducible Apple Silicon benchmark commands from the repo root. The helper should target the built `build/macos-system-release/video2x` binary first, accept an optional input clip, generate a benchmark input that is long enough to avoid misleading zero-second summaries when needed, and handle the currently observed requirement for a dummy `--output` path in benchmark mode without forcing contributors to discover that quirk themselves. Expose thin `just` recipes for at least one validated filtering path and one validated interpolation path, such as `benchmark-macos-realesrgan` and `benchmark-macos-rife`, unless the current host proves one of those paths cannot run safely.</action>
  <verify>`just --list --unsorted | rg "benchmark-macos"` shows the new benchmark recipes, `just --show benchmark-macos-realesrgan` reveals a thin delegation into the helper script, and running the helper for the filtering path exits `0`, prints a `Video2X Benchmark summary` with `Average processing speed`, and either records a non-zero elapsed time or explicitly rejects a too-short run as insufficient evidence.</verify>
  <done>The repo has a stable benchmark command surface for Apple Silicon that reuses the current benchmark mode instead of making contributors assemble one-off commands.</done>
</task>

<task type="auto">
  <name>Task 2: Record baseline Apple Silicon limitations and benchmark caveats</name>
  <files>.planning/phases/05-optimization-gate/05-01-BASELINE.md, scripts/macos_benchmark.sh</files>
  <action>Use the new benchmark workflow to capture measured or observed limitations in the current macOS path. Record the benchmark inputs, processor path, whether the run represents filtering or interpolation, the benchmark summary output, and the host caveats that affect interpretation such as duplicate MoltenVK warnings, benchmark mode's dummy-output requirement, and the distinction between benchmark throughput and end-to-end runtime proof. Treat `Total time taken: 00:00:00` as insufficient evidence: rerun with a longer clip or mark that result invalid rather than treating it as a valid baseline. The recorded evidence must include at least one real filtering-path result and one real interpolation-path result, or an explicit blocker for the interpolation path with the exact failure signal.</action>
  <verify>`rg -n "Average processing speed|realesrgan|rife|interpolation|benchmark|dummy output|MoltenVK|install_name_tool|Apple Silicon|bottleneck|limitation|blocker|invalid|too short|Total time taken" .planning/phases/05-optimization-gate/05-01-BASELINE.md` shows recorded benchmark evidence, the evidence-quality handling, and either a real interpolation result or an explicit blocker.</verify>
  <done>The project has a concrete Apple Silicon baseline document that later plans can use as evidence instead of relying on memory or scattered console output.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f scripts/macos_benchmark.sh`
- [ ] `just --list --unsorted | rg "benchmark-macos"`
- [ ] `just --show benchmark-macos-realesrgan`
- [ ] `if just --list --unsorted | rg -q "benchmark-macos-rife"; then just --show benchmark-macos-rife; fi`
- [ ] `./scripts/macos_benchmark.sh benchmark-realesrgan`
- [ ] The benchmark run above exits `0` and prints `Video2X Benchmark summary`
- [ ] The benchmark run above either reports a non-zero `Total time taken` or the workflow rejects the result as too short and reruns or records it as invalid evidence
- [ ] If the benchmark surface exposes an interpolation recipe, run it; otherwise the baseline artifact must record an explicit interpolation blocker with the exact failure signal
- [ ] `.planning/phases/05-optimization-gate/05-01-BASELINE.md` exists and records baseline evidence plus benchmark caveats
- [ ] `rg -n "Average processing speed|dummy output|MoltenVK|Apple Silicon|bottleneck|limitation|rife|interpolation|blocker|invalid|too short|Total time taken" .planning/phases/05-optimization-gate/05-01-BASELINE.md`
- [ ] `git diff -- .justfile scripts/macos_benchmark.sh .planning/phases/05-optimization-gate/05-01-BASELINE.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The repo exposes a reproducible macOS benchmark workflow around the existing benchmark mode
- Baseline evidence covers the current Apple Silicon path and records the benchmark quirks that affect interpretation
- The phase now has concrete measured or observed limitations to use in the later backend-decision gate
</success_criteria>

<output>
After completion, create `.planning/phases/05-optimization-gate/05-01-SUMMARY.md`
</output>
